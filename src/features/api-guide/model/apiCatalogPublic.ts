import { localized as l, type ApiEndpoint } from './apiCatalogTypes'

export const systemEndpoints: ApiEndpoint[] = [
  {
    method: 'GET', path: '/api/health', group: 'system', auth: 'public',
    title: l('服务健康检查', 'Service health check'),
    description: l('确认 Worker 路由可以正常响应，不访问用户数据。', 'Confirm that the Worker route responds without reading user data.'),
    request: 'No parameters', response: '200 · { ok: true }',
  },
  {
    method: 'GET', path: '/api/config', group: 'system', auth: 'public',
    title: l('读取公开运行配置', 'Read public runtime configuration'),
    description: l('获取应用名称、注册方式、功能开关和部署就绪状态。', 'Read the app name, registration mode, feature flags, and deployment readiness.'),
    request: 'No parameters', response: '200 · AppConfig',
  },
  {
    method: 'POST', path: '/api/setup', group: 'system', auth: 'public',
    title: l('首次初始化主管理员', 'Complete first-time owner setup'),
    description: l('仅在实例未初始化时创建主管理员并写入登录 Cookie。', 'Create the owner and set a login cookie only while the instance is uninitialized.'),
    request: 'JSON · displayName, password, setupToken', response: '200 · { user } + Set-Cookie',
    exampleBody: { displayName: 'Owner', password: 'strong-password', setupToken: 'setup-secret' },
    notes: [l('SETUP_TOKEN 至少 32 字节，完成初始化后应从 Worker Secret 中移除。', 'SETUP_TOKEN must be at least 32 bytes and should be removed after setup.')],
  },
  {
    method: 'POST', path: '/api/register', group: 'system', auth: 'public',
    title: l('外部注册普通账户', 'Register a public account'),
    description: l('在管理员允许注册且 Turnstile 验证通过时创建普通用户。', 'Create a regular user when public registration is enabled and Turnstile succeeds.'),
    request: 'JSON · email, displayName, password, turnstileToken', response: '201 · { user } + Set-Cookie',
    exampleBody: { email: 'user@example.com', displayName: 'User', password: 'strong-password', turnstileToken: 'turnstile-token' },
  },
  {
    method: 'GET', path: '/api/invitations/:token', group: 'system', auth: 'public',
    title: l('预览邀请链接', 'Preview an invitation'),
    description: l('读取邀请状态、域名、地址模式和账户策略，不消耗邀请次数。', 'Read invitation status, domain, address mode, and policy without consuming a use.'),
    request: 'Path · token', response: '200 · { invite }',
  },
  {
    method: 'POST', path: '/api/invitations/:token', group: 'system', auth: 'public',
    title: l('接受邀请并创建账户', 'Accept an invitation and create an account'),
    description: l('按邀请策略创建用户和邮箱地址，随后使用登录接口进入账户。', 'Create a user and mailbox under the invitation policy, then use the login endpoint to sign in.'),
    request: 'JSON · displayName, localPart?, password, turnstileToken?', response: '201 · { email }',
    exampleBody: { displayName: 'Invited User', localPart: 'user', password: 'strong-password', turnstileToken: 'turnstile-token' },
  },
  {
    method: 'GET', path: '/api/remote-images', group: 'system', auth: 'authenticated',
    title: l('代理安全远程图片', 'Proxy a safe remote image'),
    description: l('通过 Worker 拉取 HTTPS 邮件图片，限制跳转、类型和大小。', 'Fetch an HTTPS mail image through the Worker with redirect, type, and size limits.'),
    request: 'Query · url=https://images.example/image.png', response: '200 · image bytes',
    examplePath: '/api/remote-images?url=https%3A%2F%2Fimages.example%2Fimage.png', outputFile: 'image.png',
  },
  {
    method: 'POST', path: '/api/webhooks/resend', group: 'system', auth: 'webhook',
    title: l('接收 Resend 投递事件', 'Receive Resend delivery events'),
    description: l('验证 Svix 签名后更新已发送邮件的投递状态。', 'Verify the Svix signature, then update outbound delivery state.'),
    request: 'Headers · svix-id, svix-timestamp, svix-signature; JSON event', response: '200 · { ok: true }',
    exampleBody: { type: 'email.delivered', data: { email_id: 'resend_email_id' } },
    notes: [l('签名必须由 Resend 生成，示例占位值不能用于真实请求。', 'The signature must be generated by Resend; the placeholders are not valid signatures.')],
  },
]

export const authEndpoints: ApiEndpoint[] = [
  {
    method: 'GET', path: '/api/auth/linux-do', group: 'auth', auth: 'public',
    title: l('开始 Linux DO 登录', 'Start Linux DO sign-in'),
    description: l('生成 OAuth state 并重定向到 Linux DO Connect。', 'Create OAuth state and redirect to Linux DO Connect.'),
    request: 'Query · returnTo? (allowed application URL)', response: '302 · Linux DO authorization URL',
    examplePath: '/api/auth/linux-do?returnTo=https%3A%2F%2Fmail.example.com%2F',
  },
  {
    method: 'GET', path: '/api/auth/linux-do/callback', group: 'auth', auth: 'public',
    title: l('完成 Linux DO 回调', 'Complete the Linux DO callback'),
    description: l('校验 state、交换用户资料并创建或登录账户。', 'Validate state, exchange the user profile, and create or sign in the account.'),
    request: 'Query · code, state', response: '302 · returnTo or MFA login URL',
    examplePath: '/api/auth/linux-do/callback?code=oauth_code&state=oauth_state',
  },
  {
    method: 'POST', path: '/api/login', group: 'auth', auth: 'public',
    title: l('密码登录', 'Sign in with a password'),
    description: l('验证邮箱和密码；启用 MFA 时先返回二次验证挑战。', 'Verify email and password; return an MFA challenge when MFA is enabled.'),
    request: 'JSON · email, password', response: '200 · { user } or 202 · { mfaRequired, email }',
    exampleBody: { email: 'user@example.com', password: 'your-password' },
  },
  {
    method: 'POST', path: '/api/login/mfa', group: 'auth', auth: 'public',
    title: l('完成网页登录 MFA', 'Complete web-login MFA'),
    description: l('使用挑战 Cookie 和验证码或恢复码完成网页登录。', 'Use the challenge cookie plus a verification or recovery code to finish web sign-in.'),
    request: 'Cookie · omnimail_mfa_challenge; JSON · code', response: '200 · { user } + Set-Cookie',
    extraHeaders: { Cookie: 'omnimail_mfa_challenge=...' },
    exampleBody: { code: '123456' },
  },
  {
    method: 'POST', path: '/api/auth/token', group: 'auth', auth: 'public',
    title: l('签发设备令牌', 'Issue device tokens'),
    description: l('用密码和可选 MFA 签发短期 Access Token 与轮换 Refresh Token。', 'Issue a short-lived access token and rotating refresh token using a password and optional MFA.'),
    request: 'JSON · email, password, deviceName, mfaCode?', response: '200 · { accessToken, expiresIn, refreshToken, refreshExpiresIn, scopes, user }',
    exampleBody: { email: 'user@example.com', password: 'your-password', deviceName: 'My automation', mfaCode: '123456' },
  },
  {
    method: 'POST', path: '/api/auth/token/refresh', group: 'auth', auth: 'public',
    title: l('轮换设备令牌', 'Rotate device tokens'),
    description: l('使用有效 Refresh Token 同时轮换 Access Token 和 Refresh Token。', 'Use a valid refresh token to rotate both the access and refresh tokens.'),
    request: 'JSON · refreshToken', response: '200 · new token pair',
    exampleBody: { refreshToken: 'om_rt_...' },
  },
  {
    method: 'POST', path: '/api/auth/token/revoke', group: 'auth', auth: 'public',
    title: l('撤销 Refresh Token', 'Revoke a refresh token'),
    description: l('幂等撤销指定设备会话，即使已经撤销也返回成功。', 'Idempotently revoke the device session, including when it is already revoked.'),
    request: 'JSON · refreshToken', response: '200 · { ok: true }',
    exampleBody: { refreshToken: 'om_rt_...' },
  },
  {
    method: 'POST', path: '/api/auth/extension/authorize', group: 'auth', auth: 'cookie',
    title: l('创建扩展授权码', 'Create an extension authorization code'),
    description: l('由已登录网页确认扩展、回调地址和 PKCE challenge。', 'Use the signed-in website to approve the extension, redirect URI, and PKCE challenge.'),
    request: 'Same-origin Cookie; JSON · clientId, redirectUri, state, codeChallenge', response: '200 · { redirectTo }',
    exampleBody: { clientId: 'abcdefghijklmnopabcdefghijklmnop', redirectUri: 'https://abcdefghijklmnopabcdefghijklmnop.chromiumapp.org/omnimail', state: 'state_value_32_characters_long_123', codeChallenge: 'pkce_challenge_43_characters_long_value____' },
  },
  {
    method: 'POST', path: '/api/auth/extension/exchange', group: 'auth', auth: 'public',
    title: l('交换扩展授权码', 'Exchange an extension authorization code'),
    description: l('扩展使用一次性授权码和 PKCE verifier 换取受限设备令牌。', 'Exchange a one-time authorization code and PKCE verifier for scoped device tokens.'),
    request: 'Extension Origin; JSON · code, codeVerifier, clientId, redirectUri', response: '200 · scoped token pair',
    extraHeaders: { Origin: 'chrome-extension://abcdefghijklmnopabcdefghijklmnop' },
    exampleBody: { code: 'om_ac_...', codeVerifier: 'pkce_verifier_43_characters_long_value_____', clientId: 'abcdefghijklmnopabcdefghijklmnop', redirectUri: 'https://abcdefghijklmnopabcdefghijklmnop.chromiumapp.org/omnimail' },
  },
  {
    method: 'GET', path: '/api/session', group: 'auth', auth: 'optional',
    title: l('查询当前会话', 'Read the current session'),
    description: l('接受浏览器 Cookie 或 Bearer Token；未登录时 user 为 null。', 'Accept a browser cookie or bearer token; user is null when signed out.'),
    request: 'Optional · Cookie or Authorization header', response: '200 · { user: User | null }',
  },
  {
    method: 'POST', path: '/api/client-errors', group: 'auth', auth: 'authenticated',
    title: l('记录客户端异常', 'Record a client error'),
    description: l('接收已登录客户端的安全化崩溃摘要，并写入 Cloudflare Workers Logs。', 'Accept a sanitized crash summary from an authenticated client and write it to Cloudflare Workers Logs.'),
    request: 'JSON · crashId, errorName, message, componentStack, path', response: '204 · No Content',
    exampleBody: { crashId: 'ui-m123abc', errorName: 'TypeError', message: 'Render failed', componentStack: 'at Mailbox', path: '/mail/inbox' },
  },
  {
    method: 'POST', path: '/api/logout', group: 'auth', auth: 'authenticated',
    title: l('退出当前会话', 'Sign out the current session'),
    description: l('删除浏览器会话，或撤销当前 Bearer 设备会话。', 'Delete the browser session or revoke the current bearer device session.'),
    request: 'No body', response: '200 · { ok: true }',
  },
  {
    method: 'GET', path: '/api/auth/devices', group: 'auth', auth: 'authenticated',
    title: l('列出设备会话', 'List device sessions'),
    description: l('列出当前用户的设备、Scope、最近使用和撤销状态，不返回令牌明文。', 'List the current user’s devices, scopes, last use, and revocation state without plaintext tokens.'),
    request: 'No parameters', response: '200 · { devices }',
  },
  {
    method: 'DELETE', path: '/api/auth/devices/:id', group: 'auth', auth: 'authenticated',
    title: l('撤销指定设备', 'Revoke a device session'),
    description: l('撤销当前用户拥有的一个设备会话。', 'Revoke one device session owned by the current user.'),
    request: 'Path · id', response: '200 · { ok: true }',
  },
  {
    method: 'PATCH', path: '/api/account', group: 'auth', auth: 'authenticated',
    title: l('更新账户资料或密码', 'Update profile or password'),
    description: l('修改显示名称，或验证当前密码后设置新密码并撤销全部设备令牌。', 'Change the display name, or verify the current password, set a new password, and revoke device tokens.'),
    request: 'JSON · displayName?; currentPassword + newPassword?', response: '200 · { user }',
    exampleBody: { displayName: 'New Name' },
  },
  {
    method: 'DELETE', path: '/api/account', group: 'auth', auth: 'authenticated',
    title: l('注销当前账户', 'Delete the current account'),
    description: l('普通用户用登录邮箱确认；临时用户用当前密码确认。管理员不能自助注销。', 'Regular users confirm with email; temporary users confirm with password. Administrators cannot self-delete.'),
    request: 'JSON · confirmationEmail? or currentPassword?', response: '200 · { ok: true } + cleared sessions',
    exampleBody: { confirmationEmail: 'user@example.com' },
  },
  {
    method: 'GET', path: '/api/account/mfa', group: 'auth', auth: 'admin',
    title: l('读取 MFA 状态', 'Read MFA status'),
    description: l('返回 MFA 是否可配置、是否启用和剩余恢复码数量。', 'Return whether MFA is available, enabled, and how many recovery codes remain.'),
    request: 'No parameters', response: '200 · { ready, enabled, recoveryCodesRemaining }',
  },
  {
    method: 'POST', path: '/api/account/mfa/setup', group: 'auth', auth: 'admin',
    title: l('开始 MFA 配置', 'Start MFA setup'),
    description: l('生成一次性 TOTP Secret、二维码 URI 和设置期限。', 'Generate a temporary TOTP secret, QR URI, and setup expiry.'),
    request: 'No body', response: '200 · { secret, uri }',
  },
  {
    method: 'POST', path: '/api/account/mfa/confirm', group: 'auth', auth: 'admin',
    title: l('确认启用 MFA', 'Confirm MFA setup'),
    description: l('验证当前 TOTP 后启用 MFA，并仅返回一次恢复码。', 'Verify the current TOTP, enable MFA, and return recovery codes once.'),
    request: 'JSON · code', response: '200 · { enabled: true, recoveryCodes }',
    exampleBody: { code: '123456' },
  },
  {
    method: 'DELETE', path: '/api/account/mfa', group: 'auth', auth: 'admin',
    title: l('关闭 MFA', 'Disable MFA'),
    description: l('使用有效 TOTP 或恢复码关闭 MFA 并清除恢复码。', 'Disable MFA with a valid TOTP or recovery code and clear recovery codes.'),
    request: 'JSON · code', response: '200 · { enabled: false }',
    exampleBody: { code: '123456' },
  },
]
