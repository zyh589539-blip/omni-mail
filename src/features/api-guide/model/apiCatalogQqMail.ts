import { localized as l, type ApiEndpoint } from './apiCatalogTypes'

export const qqMailEndpoints: ApiEndpoint[] = [
  {
    method: 'GET', path: '/api/qq-mail/accounts', group: 'qqMail', auth: 'authenticated',
    title: l('列出 QQ 邮箱账号', 'List QQ Mail accounts'),
    description: l('返回当前用户的脱敏账号与同步状态，不返回授权码或密文。', 'Return sanitized accounts and synchronization state without authorization codes or ciphertext.'),
    request: 'No parameters', response: '200 · { enabled, accounts }',
  },
  {
    method: 'POST', path: '/api/qq-mail/accounts', group: 'qqMail', auth: 'authenticated',
    title: l('连接 QQ 邮箱账号', 'Connect a QQ Mail account'),
    description: l('验证个人 @qq.com 邮箱的授权码后，用独立密钥加密保存并请求首次同步。', 'Validate an authorization code for a personal @qq.com mailbox, encrypt it with a dedicated key, and request the initial sync.'),
    request: 'JSON · name, email, authorizationCode', response: '201 · { account }',
    exampleBody: { name: 'Personal QQ Mail', email: '123456789@qq.com', authorizationCode: 'qq-mail-authorization-code' },
    notes: [l('authorizationCode 必须是 QQ 邮箱生成的授权码，不是 QQ 登录密码。', 'authorizationCode must be generated by QQ Mail; never submit the QQ account password.')],
  },
  {
    method: 'PATCH', path: '/api/qq-mail/accounts/:id', group: 'qqMail', auth: 'authenticated',
    title: l('重命名 QQ 邮箱账号', 'Rename a QQ Mail account'),
    description: l('修改当前用户 QQ 邮箱账号的本地显示名称。', 'Change the local display name of a QQ Mail account owned by the current user.'),
    request: 'Path · id; JSON · name', response: '200 · { account }',
    exampleBody: { name: 'Personal QQ' },
  },
  {
    method: 'PUT', path: '/api/qq-mail/accounts/:id/authorization-code', group: 'qqMail', auth: 'authenticated',
    title: l('更新 QQ 邮箱授权码', 'Update a QQ Mail authorization code'),
    description: l('先验证新授权码，再替换密文；验证失败时保留原凭据。', 'Validate the new authorization code before replacing ciphertext; preserve the existing credential if validation fails.'),
    request: 'Path · id; JSON · authorizationCode', response: '200 · { account }',
    exampleBody: { authorizationCode: 'replacement-authorization-code' },
  },
  {
    method: 'DELETE', path: '/api/qq-mail/accounts/:id', group: 'qqMail', auth: 'authenticated',
    title: l('断开 QQ 邮箱账号', 'Disconnect a QQ Mail account'),
    description: l('级联删除本地密文和元数据索引，不删除远端邮件或代为撤销授权码。', 'Cascade-delete local ciphertext and metadata without deleting remote mail or revoking the authorization code.'),
    request: 'Path · id', response: '200 · { ok, remoteRevocationRequired=true }',
  },
  {
    method: 'POST', path: '/api/qq-mail/accounts/:id/verify', group: 'qqMail', auth: 'authenticated',
    title: l('验证 QQ 邮箱连接', 'Verify a QQ Mail connection'),
    description: l('使用已保存授权码重新执行只读 QQ IMAP 登录与 EXAMINE。', 'Use the saved authorization code to run read-only QQ IMAP login and EXAMINE again.'),
    request: 'Path · id', response: '200 · { ok, validatedAt }',
  },
  {
    method: 'POST', path: '/api/qq-mail/accounts/:id/sync', group: 'qqMail', auth: 'authenticated',
    title: l('请求 QQ 邮箱同步', 'Request QQ Mail synchronization'),
    description: l('在频率限制和账号租约保护下，把有限 INBOX 同步任务加入 Queue。', 'Queue a bounded INBOX synchronization under rate limiting and an account lease.'),
    request: 'Path · id; JSON · limit=10|20|50?', response: '202 · { queued: true, limit }',
    exampleBody: { limit: 20 },
  },
  {
    method: 'POST', path: '/api/qq-mail/accounts/:id/identities', group: 'qqMail', auth: 'authenticated',
    title: l('添加 QQ 邮箱发信身份', 'Add a QQ Mail sender identity'),
    description: l('使用账号现有授权码验证候选地址可登录固定 QQ SMTP 后，保存为可选发信身份。', 'Use the account’s existing authorization code to verify that the candidate address can sign in to the fixed QQ SMTP service before saving it as an optional sender identity.'),
    request: 'Path · id; JSON · name, email', response: '201 · { account }',
    exampleBody: { name: 'Foxmail address', email: 'name@foxmail.com' },
    notes: [l('只接受 @qq.com、@foxmail.com 与 @vip.qq.com；验证过程不会发送测试邮件。', 'Only @qq.com, @foxmail.com, and @vip.qq.com are accepted; verification does not send a test message.')],
  },
  {
    method: 'DELETE', path: '/api/qq-mail/accounts/:id/identities/:identityId', group: 'qqMail', auth: 'authenticated',
    title: l('删除 QQ 邮箱发信身份', 'Delete a QQ Mail sender identity'),
    description: l('删除当前账号的非主发信身份，不影响共享 INBOX、远端邮箱或主身份。', 'Delete a non-primary sender identity from the current account without affecting the shared INBOX, remote mailbox, or primary identity.'),
    request: 'Path · id, identityId', response: '200 · { account }',
  },
  {
    method: 'GET', path: '/api/qq-mail/messages', group: 'qqMail', auth: 'authenticated',
    title: l('列出 QQ 邮箱聚合邮件', 'List unified QQ Mail messages'),
    description: l('按账号或全部账号搜索 D1 元数据索引，并使用稳定游标分页。', 'Search the D1 metadata index for one or all accounts with stable cursor pagination.'),
    request: 'Query · accountId?, q?, limit=1..50?, cursor?', response: '200 · { messages, page }',
    examplePath: '/api/qq-mail/messages?limit=30',
  },
  {
    method: 'POST', path: '/api/qq-mail/accounts/:id/messages', group: 'qqMail', auth: 'authenticated',
    title: l('发送或回复 QQ 邮件', 'Send or reply with QQ Mail'),
    description: l('从当前用户已连接的 QQ 地址，通过固定官方 SMTP 端点异步发送单收件人邮件。', 'Asynchronously send a single-recipient message from the current user’s connected QQ address through the fixed official SMTP endpoint.'),
    request: 'Path · id; JSON · sender?, to, subject, text, idempotencyKey, replyToMessageId?',
    response: '202 · { message: { id, status=processing } }',
    exampleBody: { to: 'recipient@example.com', subject: 'Hello', text: 'Message body', idempotencyKey: 'request_12345678' },
    notes: [
      l('replyToMessageId 存在时，服务端从原邮件推导收件人、主题和线程头，忽略客户端伪造值。', 'When replyToMessageId is present, the server derives the recipient, subject, and thread headers from the original message and ignores spoofed client values.'),
      l('sender 必须是该账号中已通过 QQ SMTP 验证的身份；省略时使用主身份。', 'sender must be an identity verified by QQ SMTP for this account; omit it to use the primary identity.'),
      l('DATA 提交后的超时或断连会标记为投递结果不确定，不会自动重发。', 'A timeout or disconnect after DATA is marked delivery-uncertain and is never retried automatically.'),
    ],
  },
  {
    method: 'GET', path: '/api/qq-mail/accounts/:accountId/messages/:messageId', group: 'qqMail', auth: 'authenticated',
    title: l('读取 QQ 邮箱正文', 'Read a QQ Mail message'),
    description: l('重新校验归属与 UIDVALIDITY，通过 BODY.PEEK[] 按需读取正文，并独立尝试写入 Seen。', 'Revalidate ownership and UIDVALIDITY, fetch the body on demand with BODY.PEEK[], and independently attempt a Seen write.'),
    request: 'Path · accountId, messageId', response: '200 · { message }',
    examplePath: '/api/qq-mail/accounts/qq_mail_account_id/messages/message_id',
    notes: [l('已读写入失败不会阻断正文响应；移动、删除、归档、星标和其他远端写入均未开放。', 'A Seen write failure does not block the body response; move, delete, archive, star, and other remote writes are not available.')],
  },
  {
    method: 'GET', path: '/api/qq-mail/accounts/:accountId/messages/:messageId/attachments/:partId', group: 'qqMail', auth: 'authenticated',
    title: l('下载 QQ 邮箱附件', 'Download a QQ Mail attachment'),
    description: l('校验归属后按需读取并返回不超过 5 MiB 的附件。', 'Verify ownership, then fetch and return an attachment up to 5 MiB on demand.'),
    request: 'Path · accountId, messageId, partId', response: '200 · attachment bytes',
    examplePath: '/api/qq-mail/accounts/qq_mail_account_id/messages/message_id/attachments/0',
    outputFile: 'qq-mail-attachment.bin',
  },
]
